zt is a tinygrab clone written in bash for linux.
=================================================

it requires ssh keys set up properly on the local and remote machine, for passwordless login.
locally it requires xclip, and scrot, and, if you uncomment the libnotify stuff (i find it annoying), ... libnotify
